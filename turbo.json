{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "generate": {
      "dependsOn": ["^generate"],
      "outputs": ["dist/**"],
      "inputs": ["**/**/*.ts", "**/**/*.vue"]
    },
    "test": {},
    "lint": {}
  }
}
